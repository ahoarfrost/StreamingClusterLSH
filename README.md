A super simple implementation streaming LSH clustering, with naive cluster assignment

## TODO:
* process chunks of reads at a time
* write to cluster namespace in redis database or something
* reorg as clusterLSH class
